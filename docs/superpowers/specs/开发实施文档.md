# 知枢星图 - 开发实施文档

## 文档信息

| 项目   | 内容         |
| ---- | ---------- |
| 产品名称 | 知枢星图       |
| 文档类型 | 开发实施文档     |
| 版本   | v2.7.0     |
| 创建日期 | 2026-03-17 |
| 最后更新 | 2026-03-20 |

***

## 1. 开发环境配置

### 1.1 系统要求

- **操作系统：** Windows 10/11, macOS 12+, Ubuntu 20.04+
- **Python：** 3.13 (使用Anaconda)
- **Node.js：** 18+
- **包管理器：** pip, npm/pnpm

### 1.2 后端环境配置

```bash
# 进入后端目录
cd backend

# 激活Anaconda环境
conda activate base

# 安装依赖（使用已配置好的Python环境）
# 依赖已安装在用户Python目录
# 直接运行启动脚本

# 启动后端服务
# Windows: 双击 start.bat 或运行 start.ps1
start.bat

# 或手动启动
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 1.3 前端环境配置

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问地址: http://localhost:5173
```

### 1.4 环境变量配置

**后端 .env 文件（已配置）：**

```env
# 应用配置
APP_NAME=知枢星图
APP_ENV=development
DEBUG=true
SECRET_KEY=your-secret-key-here-change-in-production

# 数据库配置
DATABASE_URL=sqlite:///./data/knowledge.db

# JWT 配置
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_DAYS=7

# AI 配置（通义千问 DashScope）
OPENAI_API_KEY=your-dashscope-api-key
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL=qwen-turbo

# 向量嵌入配置（通义千问 text-embedding-v3）
DASHSCOPE_API_KEY=your-dashscope-api-key
DASHSCOPE_EMBEDDING_MODEL=text-embedding-v3

# Hugging Face 镜像配置（可选，如已使用 DashScope 可忽略）
HF_MIRROR_URL=https://hf-mirror.com
HF_CACHE_DIR=C:/temp/huggingface_cache

# 向量数据库配置（使用英文路径避免 FAISS 兼容问题）
FAISS_INDEX_PATH=C:/temp/zhishuxingtu_faiss
```

#### 安全配置说明

**API Key 安全措施：**

| 安全措施   | 说明                                          |
| ------ | ------------------------------------------- |
| 环境变量存储 | API Key 存储在后端 `.env` 文件中，通过 `os.environ` 读取 |
| 前后端分离  | 前端不包含任何 API Key，所有 AI 调用由后端完成               |
| Git 隔离 | `.env` 已加入 `.gitignore`，不会提交到仓库             |
| 日志保护   | API Key 不会出现在服务端日志中                         |

**本地开发 vs 生产部署：**

| 环境   | 配置方式                       |
| ---- | -------------------------- |
| 本地开发 | 使用 `.env` 文件，API Key 存储在本地 |
| 生产部署 | 建议使用系统环境变量，避免 `.env` 文件泄露  |

**生产环境配置示例：**

```powershell
# Windows 系统环境变量设置
setx OPENAI_API_KEY "your-api-key-here"
setx OPENAI_BASE_URL "https://dashscope.aliyuncs.com/compatible-mode/v1"
```

***

## 2. 后端开发实施

### 2.1 项目结构（已实现）

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 应用入口
│   ├── api/
│   │   ├── __init__.py
│   │   ├── deps.py             # 依赖注入（获取当前用户）
│   │   └── routes/
│   │       ├── __init__.py
│   │       ├── auth.py         # 认证路由 ✅ 已实现
│   │       ├── notes.py        # 笔记路由 ✅ 已实现
│   │       ├── folders.py      # 文件夹路由 ✅ 已实现（支持 category_id）
│   │       ├── tags.py         # 标签路由 ✅ 已实现
│   │       ├── search.py       # 搜索路由 ✅ 已实现
│   │       ├── graph.py        # 知识图谱路由 ✅ 已实现
│   │       ├── categories.py   # 分类路由 ✅ 已实现
│   │       ├── contents.py     # 内容路由 ✅ 已实现
│   │       ├── skills.py       # Skill 路由 ✅ 已实现
│   │       └── prompts.py     # 提示词路由 ✅ 已实现（V3.1完成）
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py           # 配置管理
│   │   └── security.py         # 安全相关（bcrypt密码加密）
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py             # 用户模型 ✅ 已实现
│   │   ├── note.py             # 笔记模型 ✅ 已实现
│   │   ├── folder.py           # 文件夹模型 ✅ 已实现（支持 category_id）
│   │   ├── tag.py              # 标签模型 ✅ 已实现
│   │   ├── category.py         # 分类模型 ✅ 已实现
│   │   ├── content.py          # 内容模型 ✅ 已实现
│   │   ├── skill.py            # Skill 模型 ✅ 已实现
│   │   └── prompt.py           # 提示词模型 ✅ 新增
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── user.py             # 用户 Schema
│   │   ├── note.py             # 笔记 Schema
│   │   ├── folder.py           # 文件夹 Schema（已添加 category_id）
│   │   ├── tag.py              # 标签 Schema
│   │   ├── response.py         # 响应 Schema
│   │   ├── graph.py            # 图谱 Schema
│   │   ├── search.py           # 搜索 Schema
│   │   ├── category.py         # 分类 Schema ✅ 新增
│   │   ├── content.py          # 内容 Schema ✅ 新增
│   │   ├── skill.py            # Skill Schema ✅ 新增
│   │   └── prompt.py           # 提示词 Schema ✅ 新增
│   ├── db/
│   │   ├── __init__.py
│   │   ├── base.py             # 数据库基类
│   │   └── session.py          # 数据库会话
│   ├── services/
│   │   ├── __init__.py         # 服务初始化 ✅ 已实现
│   │   ├── embedding_service.py # 文本向量化服务 ✅ 新增
│   │   └── vector_store.py     # FAISS 向量存储服务 ✅ 新增
│   └── utils/
│       └── __init__.py
├── data/                       # 数据存储目录
│   └── knowledge.db           # SQLite 数据库
├── migrate_v2_1.py            # V2.1 数据库迁移脚本 ✅ 已实现
├── migrate_prompts.py         # 提示词模块迁移脚本 ✅ 新增
├── requirements.txt
├── .env.example
├── start.bat                  # Windows启动脚本
└── start.ps1                  # PowerShell启动脚本
```

### 2.2 已实现的API端点

#### 认证 API ✅

| 方法   | 路径                 | 说明       |
| ---- | ------------------ | -------- |
| POST | /api/auth/register | 用户注册     |
| POST | /api/auth/login    | 用户登录     |
| POST | /api/auth/logout   | 用户登出     |
| GET  | /api/auth/me       | 获取当前用户信息 |

#### 笔记 API ✅

| 方法     | 路径                        | 说明                    |
| ------ | ------------------------- | --------------------- |
| GET    | /api/notes                | 获取笔记列表（支持分页、文件夹/标签过滤） |
| POST   | /api/notes                | 创建笔记                  |
| GET    | /api/notes/{id}           | 获取笔记详情                |
| PUT    | /api/notes/{id}           | 更新笔记                  |
| DELETE | /api/notes/{id}           | 删除笔记                  |
| GET    | /api/notes/{id}/backlinks | 获取反向链接                |

#### 文件夹 API ✅

| 方法     | 路径                | 说明            |
| ------ | ----------------- | ------------- |
| GET    | /api/folders      | 获取文件夹列表（树形结构） |
| POST   | /api/folders      | 创建文件夹         |
| PUT    | /api/folders/{id} | 更新文件夹         |
| DELETE | /api/folders/{id} | 删除文件夹         |

#### 标签 API ✅

| 方法     | 路径             | 说明     |
| ------ | -------------- | ------ |
| GET    | /api/tags      | 获取所有标签 |
| POST   | /api/tags      | 创建标签   |
| PUT    | /api/tags/{id} | 更新标签   |
| DELETE | /api/tags/{id} | 删除标签   |

#### 搜索 API ✅

| 方法   | 路径                            | 说明          |
| ---- | ----------------------------- | ----------- |
| GET  | /api/search?q=xxx             | 全文搜索        |
| POST | /api/search/ai                | AI 问答       |
| GET  | /api/search/vector?q=xxx\&k=5 | 向量语义搜索 ✅ 新增 |
| POST | /api/search/vector/batch      | 批量向量搜索 ✅ 新增 |
| GET  | /api/search/vector/status     | 向量服务状态 ✅ 新增 |
| POST | /api/search/vector/rebuild    | 重建向量索引 ✅ 新增 |

#### 知识图谱 API ✅

| 方法  | 路径                          | 说明     |
| --- | --------------------------- | ------ |
| GET | /api/graph/global           | 全局图谱数据 |
| GET | /api/graph/local/{note\_id} | 局部图谱数据 |

#### 提示词 API ✅ 新增

| 方法     | 路径                      | 说明       |
| ------ | ----------------------- | -------- |
| GET    | /api/prompts            | 获取提示词列表  |
| POST   | /api/prompts            | 创建提示词    |
| GET    | /api/prompts/{id}       | 获取提示词详情  |
| PUT    | /api/prompts/{id}       | 更新提示词    |
| DELETE | /api/prompts/{id}       | 删除提示词    |
| GET    | /api/prompts/templates  | 获取提示词模板  |
| POST   | /api/prompts/initialize | 初始化默认提示词 |

### 2.3 数据库模型实现（已完成）

#### users 表

```sql
CREATE TABLE users (
    id VARCHAR(36) PRIMARY KEY,
    username VARCHAR(100) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### folders 表

```sql
CREATE TABLE folders (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    parent_id VARCHAR(36),
    level INTEGER CHECK (level IN (0, 1, 2, 3)),  -- 扩展至4层
    category_id VARCHAR(36),                       -- 新增：所属分类
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (parent_id) REFERENCES folders(id),
    FOREIGN KEY (category_id) REFERENCES categories(id)
);
```

#### notes 表

```sql
CREATE TABLE notes (
    id VARCHAR(36) PRIMARY KEY,
    title VARCHAR(500) NOT NULL,
    content TEXT,
    folder_id VARCHAR(36),
    user_id VARCHAR(36),
    linked_note_ids TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (folder_id) REFERENCES folders(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### tags 表

```sql
CREATE TABLE tags (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(100) NOT NULL UNIQUE,
    color VARCHAR(7) DEFAULT '#00d4ff',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### note\_tags 表

```sql
CREATE TABLE note_tags (
    note_id VARCHAR(36),
    tag_id VARCHAR(36),
    PRIMARY KEY (note_id, tag_id),
    FOREIGN KEY (note_id) REFERENCES notes(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);
```

***

## 3. 前端开发实施

### 3.1 项目结构（已实现）

```
frontend/
├── src/
│   ├── api/                    # API 客户端
│   │   ├── index.ts           # Axios 实例配置 + 拦截器
│   │   ├── auth.ts            # 认证 API ✅
│   │   ├── notes.ts           # 笔记 API ✅
│   │   ├── folders.ts         # 文件夹 API ✅
│   │   ├── tags.ts            # 标签 API ✅
│   │   ├── search.ts          # 搜索 API ✅
│   │   ├── graph.ts           # 图谱 API ✅
│   │   ├── category.ts        # 分类 API ✅ 已更新
│   │   ├── skills.ts          # Skill API ✅ 已更新
│   │   └── prompts.ts         # 提示词 API ✅ 新增
│   ├── assets/
│   │   └── styles/
│   │       └── main.scss      # 主样式
│   ├── components/
│   │   ├── common/
│   │   │   └── Toast.vue      # Toast 通知组件 ✅
│   │   └── layout/
│   │       ├── Header.vue     # 顶部导航栏 ✅
│   │       ├── Sidebar.vue    # 侧边栏（分类切换/文件夹/标签）✅ 已重构
│   │       └── MainLayout.vue  # 主布局 ✅
│   ├── router/
│   │   └── index.ts           # 路由配置（支持个人/工作/素材路由）✅ 已更新
│   ├── stores/                 # Pinia 状态管理
│   │   ├── auth.ts            # 认证状态 ✅
│   │   ├── notes.ts           # 笔记状态 ✅
│   │   ├── folders.ts         # 文件夹状态 ✅
│   │   ├── tags.ts            # 标签状态 ✅
│   │   ├── theme.ts           # 主题状态 ✅
│   │   ├── notification.ts    # 通知状态 ✅
│   │   ├── category.ts        # 分类状态 ✅ 已更新
│   │   └── index.ts
│   ├── types/                  # TypeScript 类型定义
│   │   ├── api.ts
│   │   ├── note.ts
│   │   ├── folder.ts          # 已添加 category_id ✅
│   │   ├── tag.ts
│   │   ├── graph.ts
│   │   ├── category.ts        # 分类类型 ✅ 新增
│   │   ├── skill.ts           # Skill 类型 ✅ 新增
│   │   ├── prompt.ts          # 提示词类型 ✅ 新增
│   │   └── index.ts
│   ├── views/
│   │   ├── Login.vue          # 登录页面 ✅
│   │   ├── Home.vue           # 首页（3入口卡片）✅ 已重构
│   │   ├── NoteEditor.vue     # Markdown编辑器 ✅
│   │   ├── GraphView.vue      # 知识图谱页面 ✅
│   │   ├── SearchResults.vue   # 搜索结果页面 ✅
│   │   ├── CategoryView.vue   # 分类视图页面 ✅ 新增
│   │   ├── ContentEditor.vue  # 内容编辑器 ✅ 新增
│   │   ├── SkillsView.vue     # Skill 管理页面 ✅ 新增
│   │   └── PromptsView.vue    # 提示词管理页面 ✅ 新增
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── vite.config.ts             # Vite 配置（含API代理）
└── tsconfig.json
```

### 3.2 已实现的页面和组件

| 页面/组件             | 状态 | 说明                         |
| ----------------- | -- | -------------------------- |
| Login.vue         | ✅  | 登录页面，支持注册                  |
| Home.vue          | ✅  | 首页（3入口卡片布局：个人/工作/素材）✅ 已重构  |
| NoteEditor.vue    | ✅  | Markdown编辑器，分屏预览，双向链接      |
| GraphView\.vue    | ✅  | 知识图谱可视化页面                  |
| SearchResults.vue | ✅  | 搜索结果页面，支持AI问答              |
| CategoryView\.vue | ✅  | 分类视图页面 ✅ 新增                |
| ContentEditor.vue | ✅  | 内容编辑器 ✅ 新增                 |
| SkillsView\.vue   | ✅  | Skill 管理页面 ✅ 新增            |
| PromptsView\.vue  | ✅  | 提示词管理页面 ✅ 新增               |
| MainLayout.vue    | ✅  | 主布局，包含Header和Sidebar       |
| Sidebar.vue       | ✅  | 侧边栏（支持分类切换 + 4层嵌套文件夹）✅ 已重构 |
| Toast.vue         | ✅  | 消息通知组件                     |

***

## 4. 开发进度总结

### 4.1 MVP 功能完成状态

| 功能模块         | 状态   | 完成度  |
| ------------ | ---- | ---- |
| 用户认证         | ✅ 完成 | 100% |
| 笔记管理 CRUD    | ✅ 完成 | 100% |
| Markdown 编辑器 | ✅ 完成 | 100% |
| 文件夹管理        | ✅ 完成 | 100% |
| 标签系统         | ✅ 完成 | 100% |
| 双向链接         | ✅ 完成 | 100% |
| 反向链接         | ✅ 完成 | 100% |
| 全文搜索         | ✅ 完成 | 100% |
| 知识图谱         | ✅ 完成 | 100% |
| AI 问答        | ✅ 完成 | 100% |
| 向量语义搜索       | ✅ 完成 | 100% |

### 4.2 V2 功能完成状态

| 功能模块     | 状态   | 完成度  |
| -------- | ---- | ---- |
| 内容分类体系   | ✅ 完成 | 100% |
| 内容类型管理   | ✅ 完成 | 100% |
| 分类视图页面   | ✅ 完成 | 100% |
| 内容管理 API | ✅ 完成 | 100% |
| 内容编辑器    | ✅ 完成 | 100% |

### 4.2.1 V2.1 升级完成状态

| 功能模块                      | 状态   | 完成度  |
| ------------------------- | ---- | ---- |
| folders 表 category\_id 字段 | ✅ 完成 | 100% |
| folders 表 4 层层级支持         | ✅ 完成 | 100% |
| 主页 3 入口卡片布局               | ✅ 完成 | 100% |
| 侧边栏分类切换                   | ✅ 完成 | 100% |
| 侧边栏 4 层嵌套文件夹              | ✅ 完成 | 100% |
| 个人/工作/素材路由                | ✅ 完成 | 100% |
| category store            | ✅ 完成 | 100% |
| content store             | ✅ 完成 | 100% |
| AI 助手对话界面                 | ✅ 完成 | 100% |

### 4.2.2 V2.2 向量搜索功能完成状态

| 功能模块                            | 状态   | 完成度  |
| ------------------------------- | ---- | ---- |
| 文本向量化服务 (embedding\_service.py) | ✅ 完成 | 100% |
| FAISS 向量存储服务 (vector\_store.py) | ✅ 完成 | 100% |
| 通义千问 text-embedding-v3 API 集成   | ✅ 完成 | 100% |
| Hugging Face 国内镜像配置（可选）         | ✅ 完成 | 100% |
| 向量语义搜索 API                      | ✅ 完成 | 100% |
| 批量向量搜索 API                      | ✅ 完成 | 100% |
| 向量索引迁移脚本                        | ✅ 完成 | 100% |
| 优雅降级机制                          | ✅ 完成 | 100% |
| 功能测试用例                          | ✅ 完成 | 100% |

### 4.2.3 V2.3 向量模型升级完成状态

| 功能模块        | 状态   | 完成度  | 说明                        |
| ----------- | ---- | ---- | ------------------------- |
| 通义千问 API 切换 | ✅ 完成 | 100% | 从本地 MiniLM 升级为云端 API      |
| 向量维度升级      | ✅ 完成 | 100% | 从 384 维 → 1024 维          |
| 长文本支持       | ✅ 完成 | 100% | 从 512 tokens → 2048 字符    |
| 配置管理更新      | ✅ 完成 | 100% | 新增 DASHSCOPE\_API\_KEY 配置 |
| 文档更新        | ✅ 完成 | 100% | 技术架构文档同步更新                |

### 4.3 V3 功能完成状态

| 功能模块         | 状态   | 完成度  |
| ------------ | ---- | ---- |
| Skill 数据模型   | ✅ 完成 | 100% |
| Skill 模板库    | ✅ 完成 | 100% |
| Skill API 端点 | ✅ 完成 | 100% |
| Skill 执行引擎   | ✅ 完成 | 100% |
| Skill 管理页面   | ✅ 完成 | 100% |

### 4.3.1 V3.1 提示词工程完成状态

| 功能模块         | 状态   | 完成度  | 说明                  |
| ------------ | ---- | ---- | ------------------- |
| 提示词数据模型      | ✅ 完成 | 100% | prompts 表           |
| 提示词 Schema   | ✅ 完成 | 100% | Pydantic 模型         |
| 提示词 CRUD API | ✅ 完成 | 100% | 完整的增删改查接口           |
| 6 个预设模板      | ✅ 完成 | 100% | 知识问答、AI搜索、对话总结等     |
| 敏感词过滤        | ✅ 完成 | 100% | 自动检测并过滤             |
| 免责声明         | ✅ 完成 | 100% | AI 回复自动追加           |
| 前端管理界面       | ✅ 完成 | 100% | PromptsView\.vue    |
| 数据库迁移脚本      | ✅ 完成 | 100% | migrate\_prompts.py |

### 4.3.2 V3.2 向量长期记忆完成状态 ✅ 新增

| 功能模块              | 状态   | 完成度  | 说明                          |
| ----------------- | ---- | ---- | --------------------------- |
| 对话记忆向量存储          | ✅ 完成 | 100% | chat\_memory\_store.py      |
| 语义检索历史对话          | ✅ 完成 | 100% | 基于当前问题检索相关历史               |
| ChatChain 集成      | ✅ 完成 | 100% | 无缝集成到现有对话流程                |
| 配置参数管理            | ✅ 完成 | 100% | TOP\_K、MIN\_SCORE 等参数可配置    |
| 功能开关              | ✅ 完成 | 100% | USE\_VECTOR\_MEMORY 环境变量控制 |

### 4.4 前后端联调状态

| 测试项      | 状态                      |
| -------- | ----------------------- |
| 后端服务启动   | ✅ 正常运行 (localhost:8000) |
| 前端服务启动   | ✅ 正常运行 (localhost:5173) |
| 跨域配置     | ✅ 正确配置                  |
| 认证流程     | ✅ Token机制工作正常           |
| API接口连通性 | ✅ 全部通过                  |
| 异常处理     | ✅ 错误信息返回正确              |

### 4.3 技术实现细节

**双向链接实现：**

- 使用正则表达式 `\[\[([^\]]+)\]\]` 解析链接
- 编辑时自动解析并存储目标笔记ID
- 预览时渲染为可点击链接

**知识图谱实现：**

- 使用 vue-force-graph 库渲染
- 全局图谱显示所有笔记关系
- 局部图谱显示当前笔记的直接关联

**Markdown编辑器实现：**

- 使用 markdown-it 解析
- 分屏显示（编辑区 + 预览区）
- 同步滚动支持

***

## 5. 后续开发计划

### 5.1 V2 阶段：内容分类升级

| 功能     | 优先级 | 预计工时 |
| ------ | --- | ---- |
| 内容分类体系 | P1  | 2天   |
| 内容类型管理 | P1  | 1天   |
| 素材管理   | P1  | 2天   |
| 分类视图   | P1  | 1天   |
| 内容类型模板 | P1  | 1天   |

### 5.2 V3 阶段：Skill 智能模块

| 功能           | 优先级 | 预计工时 |
| ------------ | --- | ---- |
| Skill 创建     | P1  | 2天   |
| Skill 模板库    | P1  | 1天   |
| Skill 执行引擎   | P1  | 2天   |
| AI 生成 Skill  | P2  | 2天   |
| Skill 与知识库关联 | P1  | 1天   |

### 5.3 V4 阶段：扩展功能

| 功能     | 优先级 | 预计工时 |
| ------ | --- | ---- |
| 语义搜索   | P1  | 2天   |
| 数据导入导出 | P1  | 1天   |
| 批量导入   | P1  | 1天   |
| 网页内容提取 | P1  | 2天   |
| 浏览器插件  | P2  | 3天   |

### 5.4 V4.1 阶段：LangChain 框架集成 🚀 🚀 🚀 **优先级提升**

> **🚀 重要性声明**：LangChain 集成已提升为下一阶段开发重点。项目 `requirements.txt` 中已声明 `langchain>=0.3.0` 等依赖，但当前 RAG 实现为手工编写。为提升代码模块化程度和未来扩展能力，立即启动 LangChain 框架集成。

#### 5.4.1 LangChain 集成分析

**当前现状 vs LangChain 对应组件：**

| 当前组件                   | 技术实现                          | LangChain 对应                                         | 改造复杂度 | 优先级 |
| ---------------------- | ----------------------------- | ---------------------------------------------------- | ----- | --- |
| `embedding_service.py` | `requests` 直接调用 DashScope API | `langchain_community.embeddings.DashScopeEmbeddings` | ⭐ 中   | P1  |
| `vector_store.py`      | `faiss-cpu` 原生 API            | `langchain_community.vectorstores.FAISS`             | ⭐ 低   | P1  |
| `prompts.py` 变量替换      | 字符串 `replace()`               | `langchain.prompts.PromptTemplate`                   | ⭐ 低   | P1  |
| `search.py` AI 搜索      | 手工 prompt 拼接 + OpenAI SDK     | `langchain.chains.RetrievalQA`                       | ⭐⭐ 中高 | P1  |
| `search.py` AI 对话      | 手工 JSON 管理 history            | `langchain.chains.ConversationalRetrievalChain`      | ⭐⭐⭐ 高 | P2  |
| `skills.py` 执行         | 手工 prompt 拼接 + SDK            | `langchain.chains.LLMChain`                          | ⭐ 中   | P1  |

#### 5.4.2 分阶段实施计划（渐进式改造）

> **核心原则**：渐进式改造，保留原有实现，通过环境变量开关切换，确保不破坏现有功能。

##### 阶段 0：基础设施准备（0.5 天）

> 目标：创建 LangChain 封装层，但不修改任何现有业务逻辑

| 任务            | 新建文件                                   | 关键代码                                       | 验收标准                            |
| ------------- | -------------------------------------- | ------------------------------------------ | ------------------------------- |
| LLM 服务封装      | `app/services/llm_service.py`          | `DashScopeLLM` 单例类                         | `client.invoke(messages)` 返回结果  |
| Embeddings 封装 | `app/services/langchain_embeddings.py` | `LangChainEmbeddings(DashScopeEmbeddings)` | `embed_query("测试")` 返回 1024 维向量 |

**代码示例：**

```python
# app/services/llm_service.py
from langchain_openai import ChatOpenAI
from app.core.config import settings

class DashScopeLLM:
    _instance = None
    
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._instance._client = ChatOpenAI(
                model=settings.OPENAI_MODEL,
                api_key=settings.OPENAI_API_KEY,
                base_url=settings.OPENAI_BASE_URL,
                temperature=0.7
            )
        return cls._instance
    
    @property
    def client(self):
        return self._client
    
    def invoke(self, messages, **kwargs):
        return self._client.invoke(messages, **kwargs)

def get_llm_service() -> DashScopeLLM:
    return DashScopeLLM()
```

```python
# app/services/langchain_embeddings.py
from langchain_community.embeddings import DashScopeEmbeddings
from app.core.config import settings

class LangChainEmbeddings(DashScopeEmbeddings):
    def __init__(self):
        super().__init__(
            model=settings.DASHSCOPE_EMBEDDING_MODEL,
            dashscope_api_key=settings.DASHSCOPE_API_KEY
        )

_embedding_service = None

def get_langchain_embeddings() -> LangChainEmbeddings:
    global _embedding_service
    if _embedding_service is None:
        _embedding_service = LangChainEmbeddings()
    return _embedding_service
```

##### 阶段 1：VectorStore 封装（0.5 天）

> 目标：用 LangChain FAISS 封装层，保持与原接口兼容

| 任务                    | 新建文件                                    | 关键代码                     | 验收标准                                                    |
| --------------------- | --------------------------------------- | ------------------------ | ------------------------------------------------------- |
| LangChain VectorStore | `app/services/langchain_vectorstore.py` | `LangChainVectorStore` 类 | `add_texts()` 添加后 `similarity_search_with_score()` 返回结果 |
| 兼容层适配器                | `app/services/vector_store_adapter.py`  | `VectorStoreAdapter`     | 索引保存/加载正常                                               |

##### 阶段 2：Prompt 模板改造（0.5 天）

> 目标：将 `prompts.py` 中的变量替换改为 LangChain PromptTemplate

| 任务        | 新建文件                                        | 关键代码                      | 验收标准                                          |
| --------- | ------------------------------------------- | ------------------------- | --------------------------------------------- |
| Prompt 服务 | `app/services/prompt_service.py`            | `PromptService` 类         | `create_prompt_template()` 创建模板，`render()` 渲染 |
| 兼容层       | `prompts.py` 新增 `render_prompt_langchain()` | 与原 `render_prompt()` 输出一致 | 变量替换结果一致                                      |

##### 阶段 3：RAG 搜索改造（1 天）⚠️ 核心阶段

> 目标：用 `RetrievalQA` 替换手工 RAG 流程

| 任务           | 新建文件                            | 关键代码                    | 验收标准                                                  |
| ------------ | ------------------------------- | ----------------------- | ----------------------------------------------------- |
| RAG Chain    | `app/services/rag_chain.py`     | `RAGChain` 类            | `invoke()` 返回 `answer`, `context`, `source_documents` |
| search.py 改造 | `app/api/routes/search.py` 新增路由 | `ai_search_langchain()` | 与原 AI 搜索结果一致                                          |

##### 阶段 4：AI 对话改造（多轮对话）（1 天）

> 目标：用 `ConversationalRetrievalChain` 支持多轮对话

| 任务           | 新建文件                                     | 关键代码                               | 验收标准                              |
| ------------ | ---------------------------------------- | ---------------------------------- | --------------------------------- |
| 对话 Chain     | `app/services/chat_chain.py`             | `ChatChain`, `InMemoryChatHistory` | `invoke(query, history)` 正确处理多轮对话 |
| search.py 改造 | `app/api/routes/search.py` 新增 `/chat` 路由 | `ai_chat_langchain()`              | 历史消息正确追加                          |

##### 阶段 5：Skill 执行改造（0.5 天）

> 目标：用 `LLMChain` 简化 Skill 执行

| 任务          | 新建文件                          | 关键代码           | 验收标准                                  |
| ----------- | ----------------------------- | -------------- | ------------------------------------- |
| Skill Chain | `app/services/skill_chain.py` | `SkillChain` 类 | `execute()` 输出与原 `execute_skill()` 一致 |

#### 5.4.3 功能开关设计（渐进式切换）

为实现渐进式改造而不破坏现有功能，使用环境变量开关：

```python
# app/core/config.py 新增
class Settings(BaseSettings):
    # ... 现有配置 ...
    
    # LangChain 功能开关（默认 False，逐步开启）
    USE_LANGCHAIN_EMBEDDINGS: bool = False  # 阶段 0-1 后开启
    USE_LANGCHAIN_VECTORSTORE: bool = False  # 阶段 1 后开启
    USE_LANGCHAIN_PROMPT: bool = False  # 阶段 2 后开启
    USE_LANGCHAIN_RAG: bool = False  # 阶段 3 后开启
    USE_LANGCHAIN_CHAT: bool = False  # 阶段 4 后开启
    USE_LANGCHAIN_SKILL: bool = False  # 阶段 5 后开启
```

**search.py 路由示例（保持旧接口）：**

```python
@router.post("/ai")
async def ai_search(
    question: str,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    # 根据开关选择新旧实现
    if settings.USE_LANGCHAIN_RAG:
        return await ai_search_langchain(question, db, current_user)
    else:
        return await ai_search_original(question, db, current_user)
```

#### 5.4.4 完整实施时间表

| 阶段   | 内容                          | 工期    | 累计    | 风险等级 | 依赖      | 状态 |
| ---- | --------------------------- | ----- | ----- | ---- | ------- | ---- |
| 阶段 0 | 基础设施准备（LLM + Embeddings 封装） | 0.5 天 | 0.5 天 | 🟢 低 | 无       | ✅ 已完成 |
| 阶段 1 | VectorStore 封装              | 0.5 天 | 1 天   | 🟢 低 | 阶段 0    | ✅ 已完成 |
| 阶段 2 | Prompt 模板改造                 | 0.5 天 | 1.5 天 | 🟢 低 | 阶段 0    | ✅ 已完成 |
| 阶段 3 | RAG 搜索改造 ⚠️ 核心              | 1 天   | 2.5 天 | 🟡 中 | 阶段 1    | ✅ 已完成 |
| 阶段 4 | AI 对话改造（多轮）                 | 1 天   | 3.5 天 | 🟡 中 | 阶段 1, 2 | ✅ 已完成 |
| 阶段 5 | Skill 执行改造                  | 0.5 天 | 4 天   | 🟢 低 | 阶段 2    | ✅ 已完成 |

**🎉 LangChain 框架集成已全部完成！所有功能开关已开启并通过测试。**

#### 5.4.5 依赖确认

```env
# requirements.txt 已声明（直接使用）
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0
```

#### 5.4.6 风险评估与缓解

| 风险                  | 影响 | 可能性 | 缓解方案                             |
| ------------------- | -- | --- | -------------------------------- |
| 引入后破坏现有功能           | 高  | 低   | 渐进式改造 + 环境变量开关，保留原实现             |
| LangChain 版本升级破坏兼容性 | 中  | 中   | 锁定主版本号 `langchain>=0.3.0,<0.4.0` |
| 引入后性能下降             | 低  | 低   | LangChain 封装层极薄，几乎无额外开销          |
| 调试复杂度增加             | 中  | 中   | 每个阶段独立验收，不等问题扩大                  |

#### 5.4.7 验收检查清单

**阶段 0：**

- [x] `app/services/llm_service.py` 可导入，`DashScopeLLM().invoke(messages)` 返回结果
- [x] `app/services/langchain_embeddings.py` 可导入，`embed_query("测试")` 返回 1024 维向量

**阶段 1：**

- [x] `LangChainVectorStore.add_texts()` 可添加向量
- [x] `similarity_search_with_score()` 返回结果（id, score）
- [x] 索引保存/加载正常

**阶段 2：**

- [x] `PromptService.create_prompt_template()` 可创建模板
- [x] `render_prompt_langchain()` 输出与原 `render_prompt()` 一致

**阶段 3：**

- [x] `RAGChain.invoke()` 返回 `answer`, `context`, `source_documents`
- [x] 回答质量与原实现相当
- [x] `USE_LANGCHAIN_RAG=True` 时新旧实现结果一致

**阶段 4：**

- [x] `ChatChain.invoke(query, history)` 正确处理多轮对话
- [x] 返回的 `chat_history` 包含正确的角色信息

**阶段 5：**

- [x] `SkillChain.execute()` 输出与原 `execute_skill()` 一致

**全部完成后：**

- [x] 可通过 `USE_LANGCHAIN_*` 环境变量切换新旧实现
- [x] 全量测试通过（AI 搜索、AI 对话、Skill 执行）
- [x] 性能无明显下降

#### 5.4.8 核心代码改造示例

**改造前（当前实现）：**

```python
# embedding_service.py - 手工实现
def embed_text(self, text: str) -> np.ndarray:
    response = requests.post(url, headers=headers, json=payload)
    embedding = np.array(response.json()['data'][0]['embedding'])
    return embedding
```

**改造后（LangChain 实现）：**

```python
# langchain_embeddings.py - LangChain 封装
from langchain_community.embeddings import DashScopeEmbeddings

class LangChainEmbeddings(DashScopeEmbeddings):
    def __init__(self):
        super().__init__(
            model="text-embedding-v3",
            dashscope_api_key=settings.DASHSCOPE_API_KEY
        )
```

**RAG 问答改造示例：**

```python
# search.py - 使用 LangChain RetrievalQA
from langchain.chains import RetrievalQA
from langchain_community.vectorstores import FAISS
from app.services.langchain_embeddings import LangChainEmbeddings

def build_rag_chain(vector_store: FAISS):
    embeddings = LangChainEmbeddings()
    llm = DashScopeLLM(
        api_key=settings.OPENAI_API_KEY,
        model=settings.OPENAI_MODEL
    )
    return RetrievalQA.from_chain_type(
        llm=llm,
        chain_type="stuff",
        retriever=vector_store.as_retriever(search_kwargs={"k": 5})
    )
```

***

## 6. 测试计划

### 6.1 后端测试

```bash
# 启动后端服务后，测试各API端点
# 基础URL: http://localhost:8000

# 1. 健康检查
curl http://localhost:8000/health

# 2. 用户注册
curl -X POST http://localhost:8000/api/auth/register ^
     -H "Content-Type: application/json" ^
     -d "{\"username\":\"testuser\",\"password\":\"test123\"}"

# 3. 用户登录
curl -X POST http://localhost:8000/api/auth/login ^
     -d "username=testuser&password=test123"

# 4. 获取笔记列表（需Token）
curl http://localhost:8000/api/notes ^
     -H "Authorization: Bearer YOUR_TOKEN"
```

### 6.2 前端测试

```bash
# 访问 http://localhost:5173 进行手动测试

# 测试流程：
# 1. 登录/注册用户
# 2. 创建笔记
# 3. 编辑笔记内容（包含双向链接）
# 4. 查看笔记列表
# 5. 搜索笔记
# 6. 查看知识图谱
# 7. 测试AI问答
```

***

## 7. 常见问题排查

### 7.1 端口被占用

```powershell
# 查看端口占用
netstat -ano | Select-String ":8000"
netstat -ano | Select-String ":5173"

# 关闭占用进程
taskkill /F /PID <PID>

# 或更换端口
# 后端: python -m uvicorn app.main:app --port 8001
# 前端: npm run dev -- --port 3000
```

### 7.2 数据库问题

```powershell
# 如需重置数据库
del backend\data\knowledge.db
# 重启后端会自动创建新数据库
```

### 7.3 跨域问题

确保后端 CORS 配置包含前端地址：

```python
allow_origins=["http://localhost:5173", "http://127.0.0.1:5173", "http://localhost:3000"]
```

### 7.4 Python 环境问题

如遇模块导入错误，检查 Python 环境：

```powershell
# 确认使用的是正确的 Python 环境
where python
python --version

# 安装缺失的依赖
pip install fastapi uvicorn sqlalchemy pydantic python-jose bcrypt
```

***

## 附录：项目启动快速指南

### 启动后端

```powershell
cd d:\知枢星图\backend
start.bat
```

或手动启动：

```powershell
cd d:\知枢星图\backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 启动前端

```powershell
cd d:\知枢星图\frontend
npm run dev
```

### 访问应用

- 前端：<http://localhost:5173>
- 后端API：<http://localhost:8000>
- 健康检查：<http://localhost:8000/health>

***

## 文档历史

| 版本     | 日期         | 作者   | 变更说明                                            |
| ------ | ---------- | ---- | ----------------------------------------------- |
| v1.0.0 | 2026-03-17 | 开发团队 | 初始版本                                            |
| v1.1.0 | 2026-03-18 | 开发团队 | 更新项目进度，完成MVP功能清单，添加联调测试结果                       |
| v1.4.0 | 2026-03-18 | 开发团队 | 主页布局重构、文件夹层级扩展、分类体系设计                           |
| v2.1.0 | 2026-03-18 | 开发团队 | **V2.1升级完成** - 后端扩展、前端重构、4层文件夹、分类切换             |
| v2.2.0 | 2026-03-19 | 开发团队 | **向量搜索功能完成** - FAISS集成、HF镜像配置、语义搜索API           |
| v2.2.1 | 2026-03-19 | 开发团队 | **安全配置完善** - API Key 安全措施文档更新                   |
| v2.3.0 | 2026-03-19 | 开发团队 | **AI助手与Skill功能完成** - AI对话界面、Skill智能模块、模板库       |
| v2.3.1 | 2026-03-19 | 开发团队 | **向量模型升级** - 从 MiniLM 升级为通义千问 text-embedding-v3 |
| v2.3.2 | 2026-03-19 | 开发团队 | **提示词工程完善** - 提示词管理模块、敏感词过滤、免责声明、6个预设模板         |
| v2.4.0 | 2026-03-19 | 开发团队 | **LangChain 集成计划** - 渐进式改造计划、优先级提升、5阶段实施路线图     |
| v2.5.0 | 2026-03-20 | 开发团队 | **向量长期记忆** - 基于 FAISS 的对话记忆向量存储，语义检索历史对话        |

***

## 8. V2.1 升级设计（主页重构 + 文件夹扩展）

### 8.1 设计目标

本次升级实现：

- 主页简化为 3 个分类入口（个人/工作/素材）
- 文件夹层级从 2 层扩展至 4 层
- 各分类支持不同的内容类型和存储方式
- AI 助手作为「个人」入口内的功能模块

### 8.2 主页布局设计

```
┌─────────────────────────────────────────────────────────┐
│                     主页入口                              │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐│
│  │             │    │             │    │             ││
│  │    个人     │    │    工作     │    │    素材     ││
│  │   Personal  │    │    Work     │    │   Assets    ││
│  │             │    │             │    │             ││
│  └─────────────┘    └─────────────┘    └─────────────┘│
└─────────────────────────────────────────────────────────┘
```

### 8.3 「个人」入口结构

```
个人 (Personal)
├── 📁 笔记文件夹        # 支持 3-4 层嵌套
├── 🔍 智能检索          # 全文搜索 + AI 问答
├── 🔗 知识图谱          # 全局/局部图谱
└── 🤖 AI 助手           # 独立对话界面
```

### 8.4 「工作」入口结构

```
工作 (Work)
├── 📂 项目 A
│   ├── 📋 项目文档
│   ├── ✅ 任务列表
│   └── 📊 里程碑
├── 📂 项目 B
└── 📂 项目 C
```

### 8.5 「素材」入口结构

```
素材 (Assets)
├── 🖼️ 图片
├── 📎 附件
└── 🔗 链接收藏
```

### 8.6 文件夹层级扩展

**原有设计：** 最多 2 层（Level 0 顶层 + Level 1 子文件夹）

**新设计：** 最多 4 层（Level 0 → Level 1 → Level 2 → Level 3）

```
个人
├── 笔记文件夹 A（Level 0）
│   ├── 子文件夹 A1（Level 1）
│   │   ├── 子子文件夹 A1a（Level 2）
│   │   │   └── 深层文件夹 A1a1（Level 3）
│   │   └── 子子文件夹 A1b（Level 2）
│   └── 子文件夹 A2（Level 1）
└── 笔记文件夹 B（Level 0）
```

***

## 9. 后端数据模型更新

### 9.1 folders 表更新

**变更内容：** `level` 字段从 `CHECK (level IN (0, 1))` 改为 `CHECK (level IN (0, 1, 2, 3))`

```sql
CREATE TABLE folders (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    parent_id VARCHAR(36),
    level INTEGER CHECK (level IN (0, 1, 2, 3)),  -- 扩展至4层
    category_id VARCHAR(36),                       -- 新增：所属分类
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (parent_id) REFERENCES folders(id),
    FOREIGN KEY (category_id) REFERENCES categories(id)
);
```

### 9.2 categories 表（内容分类）

```sql
CREATE TABLE categories (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    icon VARCHAR(50),
    color VARCHAR(7) DEFAULT '#00d4ff',
    sort_order INTEGER DEFAULT 0,
    is_system BOOLEAN DEFAULT FALSE,  -- 系统预设分类不可删除
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**预置数据：**

| 分类ID     | 名称 | 图标 | 系统预设 |
| -------- | -- | -- | ---- |
| personal | 个人 | 👤 | 是    |
| work     | 工作 | 💼 | 是    |
| assets   | 素材 | 📦 | 是    |

### 9.3 content\_types 表（内容类型）

```sql
CREATE TABLE content_types (
    id VARCHAR(36) PRIMARY KEY,
    category_id VARCHAR(36) NOT NULL,
    name VARCHAR(100) NOT NULL,
    icon VARCHAR(50),
    color VARCHAR(7) DEFAULT '#00d4ff',
    field_schema TEXT,              -- JSON Schema，定义扩展字段
    is_system BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (category_id) REFERENCES categories(id)
);
```

**预置数据：**

| 分类 | 内容类型 | 图标  |
| -- | ---- | --- |
| 个人 | 笔记   | 📝  |
| 个人 | 日记   | 📅  |
| 个人 | 简历   | 📄  |
| 工作 | 项目文档 | 📋  |
| 工作 | 任务列表 | ✅   |
| 工作 | 里程碑  | 📊  |
| 素材 | 图片   | 🖼️ |
| 素材 | 附件   | 📎  |
| 素材 | 链接收藏 | 🔗  |

### 9.4 contents 表（统一内容表）

```sql
CREATE TABLE contents (
    id VARCHAR(36) PRIMARY KEY,
    type_id VARCHAR(36) NOT NULL,          -- 内容类型ID
    category_id VARCHAR(36) NOT NULL,      -- 所属分类ID
    title VARCHAR(500) NOT NULL,
    content TEXT,
    extra_fields TEXT,                     -- JSON，存储扩展字段
    linked_content_ids TEXT,              -- JSON，关联的内容ID列表
    file_path VARCHAR(500),                -- 本地文件路径
    file_url VARCHAR(1000),               -- 外部链接
    file_size INTEGER,                     -- 文件大小
    folder_id VARCHAR(36),                 -- 所属文件夹
    user_id VARCHAR(36),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (type_id) REFERENCES content_types(id),
    FOREIGN KEY (category_id) REFERENCES categories(id),
    FOREIGN KEY (folder_id) REFERENCES folders(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 9.5 分类视图 API 设计

#### 分类 API

| 方法     | 路径                   | 说明     |
| ------ | -------------------- | ------ |
| GET    | /api/categories      | 获取所有分类 |
| POST   | /api/categories      | 创建分类   |
| PUT    | /api/categories/{id} | 更新分类   |
| DELETE | /api/categories/{id} | 删除分类   |

#### 内容类型 API

| 方法     | 路径                                   | 说明           |
| ------ | ------------------------------------ | ------------ |
| GET    | /api/categories/{category\_id}/types | 获取分类下的所有内容类型 |
| POST   | /api/types                           | 创建内容类型       |
| PUT    | /api/types/{id}                      | 更新内容类型       |
| DELETE | /api/types/{id}                      | 删除内容类型       |

#### 内容 API

| 方法     | 路径                 | 说明                    |
| ------ | ------------------ | --------------------- |
| GET    | /api/contents      | 获取内容列表（支持分类/类型/文件夹过滤） |
| POST   | /api/contents      | 创建内容                  |
| GET    | /api/contents/{id} | 获取内容详情                |
| PUT    | /api/contents/{id} | 更新内容                  |
| DELETE | /api/contents/{id} | 删除内容                  |

***

## 10. 前端路由设计

### 10.1 新路由结构

```
/                           # 主页（3个入口）
├── /personal               # 个人入口
│   ├── /personal/folders   # 笔记文件夹
│   ├── /personal/search    # 智能检索
│   ├── /personal/graph     # 知识图谱
│   └── /personal/ai        # AI 助手
├── /work                   # 工作入口
│   └── /work/project/:id   # 项目详情
├── /assets                 # 素材入口
│   └── /assets/:type       # 素材类型（图片/附件/链接）
└── /editor/:id             # 编辑器（通用）
```

### 10.2 侧边栏交互设计

```
┌─────────────────────────────────────────────┐
│  [分类切换]                                  │
│  ┌─────┬─────┬─────┐                       │
│  │个人 │工作 │素材 │                       │
│  └─────┴─────┴─────┘                       │
├─────────────────────────────────────────────┤
│  [当前分类内容]                              │
│  ▼ 笔记文件夹                               │
│    ▼ 子文件夹 A                             │
│      ▼ 子子文件夹                           │
│        - 笔记 1                             │
│        - 笔记 2                             │
│    ► 文件夹 B                               │
│  [其他功能入口]                              │
│    🔍 智能检索                              │
│    🔗 知识图谱                              │
│    🤖 AI 助手                               │
└─────────────────────────────────────────────┘
```

***

## 11. 实施计划

### 11.1 第一阶段：后端扩展 ✅ 已完成

| 任务                                | 状态   | 完成日期       |
| --------------------------------- | ---- | ---------- |
| 数据库迁移：folders 表增加 category\_id 字段 | ✅ 完成 | 2026-03-18 |
| 数据库迁移：level 字段扩展至 4 层             | ✅ 完成 | 2026-03-18 |
| 创建 categories 表并插入预置数据            | ✅ 完成 | 2026-03-18 |
| 创建 content\_types 表并插入预置数据        | ✅ 完成 | 2026-03-18 |
| 创建 contents 表                     | ✅ 完成 | 2026-03-18 |
| 新增分类管理 API                        | ✅ 完成 | 2026-03-18 |
| 新增内容类型 API                        | ✅ 完成 | 2026-03-18 |
| 新增内容管理 API                        | ✅ 完成 | 2026-03-18 |
| 文件夹 API 适配 category\_id           | ✅ 完成 | 2026-03-18 |

### 11.2 第二阶段：前端重构 ✅ 已完成

| 任务                             | 状态   | 完成日期       |
| ------------------------------ | ---- | ---------- |
| 主页重构：3入口卡片布局                   | ✅ 完成 | 2026-03-18 |
| 侧边栏重构：支持分类切换 + 嵌套文件夹           | ✅ 完成 | 2026-03-18 |
| 路由配置：新增个人/工作/素材路由              | ✅ 完成 | 2026-03-18 |
| 状态管理：新增 category、content store | ✅ 完成 | 2026-03-18 |
| 笔记编辑器适配新文件夹结构                  | ✅ 完成 | 2026-03-18 |
| 工作/素材入口页面开发                    | ✅ 完成 | 2026-03-18 |

### 11.3 第三阶段：AI 助手集成 ✅ 已完成

| 任务          | 状态   | 依赖                |
| ----------- | ---- | ----------------- |
| AI 助手对话界面开发 | ✅ 完成 | 需要 OpenAI API Key |
| AI 助手与笔记联动  | ✅ 完成 | 依赖上一项             |

***

## 13. Skill 智能模块使用指南

### 13.1 功能概述

Skill 智能模块是 V3 阶段的核心功能，允许用户创建和管理自动化技能，将知识处理流程自动化。

### 13.2 访问入口

- 路由：`/skills`
- 位置：主页 → 个人入口 → AI助手（附近）或直接访问

### 13.3 功能特性

#### 模板库（预置 4 个模板）

| 模板名称 | 图标 | 功能描述          | 触发类型 |
| ---- | -- | ------------- | ---- |
| 对话总结 | 💬 | 总结对话内容并归档     | 手动   |
| 每日资讯 | 📰 | 定时汇总资讯并归档     | 定时   |
| 简历解析 | 📄 | 解析简历内容，提取关键信息 | 手动   |
| 知识卡片 | 🎴 | 自动生成知识点卡片     | 手动   |

#### 我的 Skills

- 查看已创建的 Skills
- 按类型筛选（全部/手动/定时）
- 启用/停用 Skill
- 删除 Skill
- 执行 Skill

### 13.4 使用流程

#### 方式一：从模板创建

1. 进入 Skill 管理页面（`/skills`）
2. 在「模板库」区域，点击想要的模板卡片
3. 系统自动创建 Skill 并添加到「我的 Skills」列表
4. 可选择启用并执行

#### 方式二：自定义创建

1. 点击「创建 Skill」按钮
2. 填写表单：
   - Skill 名称
   - 描述
   - 图标（默认 ⚡）
   - 颜色
   - 触发类型（手动/定时）
   - 输出分类（可选，指定结果保存到哪个分类）
3. 点击「创建」

#### 执行 Skill

1. 在「我的 Skills」列表中找到目标 Skill
2. 点击「执行」按钮
3. 在弹出的对话框中输入 JSON 格式的输入数据
4. 点击「执行」按钮
5. 查看执行结果

### 13.5 后端 API

| 方法     | 路径                                | 说明          |
| ------ | --------------------------------- | ----------- |
| GET    | /api/skills/templates             | 获取模板库       |
| GET    | /api/skills                       | 获取用户 Skills |
| GET    | /api/skills/{id}                  | 获取单个 Skill  |
| POST   | /api/skills                       | 创建 Skill    |
| POST   | /api/skills/from-template/{index} | 从模板创建       |
| PUT    | /api/skills/{id}                  | 更新 Skill    |
| DELETE | /api/skills/{id}                  | 删除 Skill    |
| POST   | /api/skills/{id}/execute          | 执行 Skill    |
| GET    | /api/skills/{id}/executions       | 获取执行历史      |

***

## 14. AI 助手使用指南

### 14.1 功能概述

AI 助手是一个基于知识库的智能对话系统，可以回答关于笔记内容的问题，并提供相关笔记链接。

### 14.2 访问入口

- 路由：`/personal/ai`
- 位置：主页 → 个人入口 → AI助手

### 14.3 功能特性

- **多会话管理**：支持创建、切换、删除多个对话
- **快捷入口**：提供 3 个预设问题快速提问
- **笔记联动**：回答内容中包含相关笔记链接
- **本地存储**：对话历史保存在浏览器 localStorage

### 14.4 使用流程

1. 进入 AI 助手页面
2. 点击「快捷入口」按钮或直接在输入框提问
3. 等待 AI 生成回答
4. 点击相关笔记链接跳转到对应笔记

### 14.5 注意事项

- 需要配置 OpenAI API Key（见环境变量配置）
- 对话历史保存在浏览器本地，清除浏览器数据会丢失

***

## 15. 提示词管理使用指南 ✅ 新增

### 15.1 功能概述

提示词管理模块是 V3.1 阶段的核心功能，允许用户创建和管理 AI 提示词模板，优化 AI 输出质量。

### 15.2 访问入口

- 路由：`/prompts`
- 位置：侧边栏 → 设置区域或通过 AI 助手页面进入

### 15.3 功能特性

#### 模板库

| 模板名称   | 分类         | 功能描述             |
| ------ | ---------- | ---------------- |
| 知识问答   | ai\_chat   | 基于知识库的智能问答助手     |
| AI搜索问答 | ai\_search | 用于全局搜索的 AI 问答提示词 |
| 对话总结   | skill      | 总结对话内容并归档        |
| 每日资讯   | skill      | 定时汇总资讯并归档        |
| 简历解析   | skill      | 解析简历内容，提取关键信息    |
| 知识卡片   | skill      | 自动生成知识点卡片        |

#### 我的提示词

- 查看已创建的提示词（系统预设 + 用户自定义）
- 按分类筛选（全部/AI对话/AI搜索/技能模板/自定义）
- 启用/停用提示词
- 编辑/删除用户自定义提示词
- 从模板快速创建

### 15.4 使用流程

#### 方式一：从模板创建

1. 进入提示词管理页面（`/prompts`）
2. 在「模板库」区域，点击想要的模板卡片右侧的「使用」按钮
3. 系统自动创建提示词并添加到「我的提示词」列表

#### 方式二：自定义创建

1. 点击「创建提示词」按钮
2. 填写表单：
   - 提示词名称
   - 描述
   - 分类（AI对话/AI搜索/技能模板/自定义）
   - 系统提示词（支持变量替换）
   - 用户提示词模板（可选）
   - 输出格式说明
3. 点击「创建」

### 15.5 变量说明

支持以下变量，占位符格式为 `{variable_name}`：

| 变量名              | 说明      | 适用场景     |
| ---------------- | ------- | -------- |
| current\_date    | 当前日期    | 所有场景     |
| current\_weekday | 当前星期    | 所有场景     |
| context          | 知识库相关内容 | AI 对话/搜索 |
| question         | 用户问题    | AI 对话/搜索 |
| conversation     | 对话内容    | 对话总结     |
| content          | 知识内容    | 知识卡片     |
| resume\_text     | 简历文本    | 简历解析     |
| sources          | 资讯来源    | 每日资讯     |
| keywords         | 关键词     | 每日资讯     |

### 15.6 敏感词过滤

系统自动检测输入内容中的敏感词，包括但不限于：

- 暴力、色情、赌博、毒品、犯罪、欺诈相关内容

检测到敏感词时，系统会拒绝处理并提示用户调整内容。

### 15.7 免责声明

所有 AI 回复会自动追加免责声明：

> **免责声明**：以上内容仅供参考，不构成任何专业意见。如有医疗、法律、金融等领域的具体问题，请咨询相关专业人士。

***

## 12. 风险评估

| 风险          | 影响 | 可能性 | 缓解方案         |
| ----------- | -- | --- | ------------ |
| 数据库迁移失败     | 高  | 低   | 提前备份，编写回滚脚本  |
| 前端侧边栏重构复杂度高 | 中  | 中   | 分步实施，先保证核心功能 |
| 多分类内容关联查询性能 | 中  | 低   | 合理建索引，优化查询语句 |

***

## 16. 提示词工程进阶实施计划 (V3.3) ✅ 新增

### 16.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    提示词工程平台                         │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Few-Shot     │  │ CoT思维链     │  │ 提示词链     │  │
│  │ 示例库       │  │ 模板引擎      │  │ 编排引擎     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │          A/B测试框架 + 效果评估系统                │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ • 版本管理  • 流量分配  • 效果对比  • 自动评估    │  │
│  └──────────────────────────────────────────────────┘  │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

### 16.2 Phase 1: Few-Shot示例库 + A/B测试基础（2-3天）

#### Day 1: 数据库与后端API

**任务清单：**
- [ ] 创建数据表
  - `few_shot_examples` - Few-Shot示例表
  - `prompt_versions` - 提示词版本表
  - `ab_experiments` - A/B实验表
  - `ab_test_results` - A/B测试结果表

**实施步骤：**

1. **创建数据模型** (`backend/app/models/few_shot.py`)
```python
from sqlalchemy import Column, String, Text, Float, Boolean, DateTime, ForeignKey
from sqlalchemy.orm import relationship
from app.db.base import Base
from datetime import datetime
import uuid

class FewShotExample(Base):
    __tablename__ = "few_shot_examples"
    
    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    prompt_id = Column(String(36), ForeignKey("prompts.id"))
    scenario = Column(String(50))  # conversation_summary/knowledge_extraction/knowledge_qa
    input_example = Column(Text)
    output_example = Column(Text)
    quality_score = Column(Float, default=5.0)
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    
    prompt = relationship("Prompt", back_populates="few_shot_examples")
```

2. **创建Schema** (`backend/app/schemas/few_shot.py`)
```python
from pydantic import BaseModel
from typing import Optional
from datetime import datetime

class FewShotExampleBase(BaseModel):
    prompt_id: str
    scenario: str
    input_example: str
    output_example: str
    quality_score: float = 5.0

class FewShotExampleCreate(FewShotExampleBase):
    pass

class FewShotExampleUpdate(BaseModel):
    input_example: Optional[str] = None
    output_example: Optional[str] = None
    quality_score: Optional[float] = None
    is_active: Optional[bool] = None

class FewShotExampleInDB(FewShotExampleBase):
    id: str
    is_active: bool
    created_at: datetime
    
    class Config:
        from_attributes = True
```

3. **创建API路由** (`backend/app/api/routes/few_shot.py`)
```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from typing import List
from app.api.deps import get_db, get_current_user
from app.models.few_shot import FewShotExample
from app.schemas.few_shot import FewShotExampleCreate, FewShotExampleUpdate, FewShotExampleInDB

router = APIRouter()

@router.post("/", response_model=FewShotExampleInDB)
def create_few_shot_example(
    example: FewShotExampleCreate,
    db: Session = Depends(get_db),
    current_user = Depends(get_current_user)
):
    """创建Few-Shot示例"""
    db_example = FewShotExample(**example.dict())
    db.add(db_example)
    db.commit()
    db.refresh(db_example)
    return db_example

@router.get("/", response_model=List[FewShotExampleInDB])
def list_few_shot_examples(
    scenario: str = None,
    prompt_id: str = None,
    db: Session = Depends(get_db)
):
    """获取Few-Shot示例列表"""
    query = db.query(FewShotExample).filter(FewShotExample.is_active == True)
    if scenario:
        query = query.filter(FewShotExample.scenario == scenario)
    if prompt_id:
        query = query.filter(FewShotExample.prompt_id == prompt_id)
    return query.all()
```

#### Day 2: A/B测试框架

**任务清单：**
- [ ] 实现A/B测试API
- [ ] 实现流量分配逻辑
- [ ] 添加测试结果记录

**实施步骤：**

1. **创建A/B测试模型** (`backend/app/models/ab_test.py`)
```python
class ABExperiment(Base):
    __tablename__ = "ab_experiments"
    
    id = Column(String(36), primary_key=True)
    name = Column(String(200))
    description = Column(Text)
    prompt_id = Column(String(36), ForeignKey("prompts.id"))
    version_a_id = Column(String(36), ForeignKey("prompt_versions.id"))
    version_b_id = Column(String(36), ForeignKey("prompt_versions.id"))
    traffic_split = Column(Float, default=0.5)
    status = Column(String(20), default='running')
    start_time = Column(DateTime)
    end_time = Column(DateTime)
    created_at = Column(DateTime, default=datetime.utcnow)
```

2. **实现流量分配** (`backend/app/services/ab_test_service.py`)
```python
import hashlib

class ABTestService:
    @staticmethod
    def assign_version(experiment_id: str, session_id: str, traffic_split: float) -> str:
        """基于session_id分配版本"""
        hash_value = int(hashlib.md5(f"{experiment_id}{session_id}".encode()).hexdigest(), 16)
        return "A" if (hash_value % 100) / 100 < traffic_split else "B"
```

#### Day 3: 前端界面与测试

**任务清单：**
- [ ] Few-Shot示例管理界面
- [ ] A/B测试管理界面
- [ ] 集成测试

**验收标准：**
- [ ] Few-Shot示例可正常CRUD
- [ ] A/B测试可正常创建和运行
- [ ] 三个核心场景有示例数据
- [ ] 流量分配准确

### 16.3 Phase 2: CoT思维链 + 效果评估系统（3-4天）

#### Day 4-5: CoT模板引擎

**任务清单：**
- [ ] 实现CoT模板引擎
- [ ] 创建3种CoT模板
- [ ] 集成到提示词系统

**实施步骤：**

1. **创建CoT引擎** (`backend/app/services/cot_engine.py`)
```python
COT_TEMPLATES = {
    "step_by_step": """
请按照以下步骤思考并回答：

步骤1：理解问题
- 分析问题的核心需求
- 识别关键信息点

步骤2：信息检索
- 从知识库中提取相关信息
- 评估信息的可靠性

步骤3：推理分析
- 基于信息进行逻辑推理
- 考虑多种可能性

步骤4：得出结论
- 总结推理结果
- 给出明确答案

现在请回答：{question}
    """,
    
    "compare_analyze": """
请使用对比分析法回答：

## 分析框架
1. **维度识别**：列出需要对比的关键维度
2. **信息提取**：从知识库提取每个维度的信息
3. **对比分析**：逐维度对比差异
4. **综合评估**：给出整体建议

问题：{question}
知识库内容：{context}

请开始分析：
    """,
    
    "deep_reasoning": """
请进行深度推理：

## 推理链
1. **问题分解**：将复杂问题拆解为子问题
2. **假设生成**：提出可能的假设
3. **假设验证**：用知识库内容验证假设
4. **结论推导**：基于验证结果得出结论

问题：{question}
相关内容：{context}

推理过程：
    """
}

class CoTEngine:
    @staticmethod
    def apply_cot(prompt: str, cot_type: str, **kwargs) -> str:
        """应用思维链模板"""
        if cot_type not in COT_TEMPLATES:
            return prompt
        
        cot_template = COT_TEMPLATES[cot_type]
        cot_prompt = cot_template.format(**kwargs)
        
        return f"{prompt}\n\n{cot_prompt}"
```

#### Day 6-7: 效果评估系统

**任务清单：**
- [ ] 实现自动评估引擎
- [ ] 创建评估数据表
- [ ] 前端效果评估仪表盘
- [ ] A/B测试结果可视化

**实施步骤：**

1. **创建评估引擎** (`backend/app/services/prompt_evaluator.py`)
```python
class PromptEvaluator:
    def evaluate_response(self, question: str, response: str, context: str) -> dict:
        """自动评估AI回答质量"""
        # 1. 相关性评估
        relevance = self._evaluate_relevance(question, response)
        
        # 2. 准确性评估（基于知识库）
        accuracy = self._evaluate_accuracy(response, context)
        
        # 3. 完整性评估
        completeness = self._evaluate_completeness(question, response)
        
        # 4. 清晰度评估
        clarity = self._evaluate_clarity(response)
        
        return {
            "relevance": relevance,
            "accuracy": accuracy,
            "completeness": completeness,
            "clarity": clarity,
            "overall_score": (relevance + accuracy + completeness + clarity) / 4
        }
    
    def _evaluate_relevance(self, question: str, response: str) -> float:
        """评估相关性"""
        # 使用LLM评估
        eval_prompt = f"""
请评估以下AI回答与问题的相关性（1-10分）：

问题：{question}
回答：{response}

只输出分数（1-10的数字）：
"""
        score = self.llm.invoke(eval_prompt)
        return float(score.content.strip())
```

**验收标准：**
- [ ] CoT模板可正常使用
- [ ] 自动评估准确率>80%
- [ ] 效果评估仪表盘可用
- [ ] A/B测试结果可视化

### 16.4 Phase 3: 提示词链编排引擎（3-5天）

#### Day 8-10: 提示词链引擎

**任务清单：**
- [ ] 实现提示词链引擎
- [ ] 创建链式编排API
- [ ] 前端可视化编排界面
- [ ] 预设链模板

**实施步骤：**

1. **创建提示词链引擎** (`backend/app/services/prompt_chain.py`)
```python
class PromptChain:
    """提示词链：多步骤任务编排"""
    
    def __init__(self):
        self.steps = []
    
    def add_step(self, prompt_id: str, input_mapping: dict, output_key: str):
        """添加步骤"""
        self.steps.append({
            "prompt_id": prompt_id,
            "input_mapping": input_mapping,
            "output_key": output_key
        })
        return self
    
    def execute(self, initial_input: dict) -> dict:
        """执行链式调用"""
        context = initial_input.copy()
        
        for step in self.steps:
            # 1. 准备输入
            step_input = {}
            for var, source in step["input_mapping"].items():
                if source.startswith("$"):
                    step_input[var] = context.get(source[1:])
                else:
                    step_input[var] = source
            
            # 2. 调用AI
            result = self._call_ai(step["prompt_id"], step_input)
            
            # 3. 保存输出
            context[step["output_key"]] = result
        
        return context
```

2. **创建预设链模板**
```python
PRESET_CHAINS = {
    "knowledge_analysis": {
        "name": "资料分析链",
        "steps": [
            {"prompt_id": "knowledge_extraction", "input_mapping": {"content": "$raw_content"}, "output_key": "extracted"},
            {"prompt_id": "knowledge_card", "input_mapping": {"content": "$extracted"}, "output_key": "card"}
        ]
    },
    "conversation_summary": {
        "name": "对话总结链",
        "steps": [
            {"prompt_id": "conversation_summary", "input_mapping": {"conversation": "$conversation"}, "output_key": "summary"}
        ]
    }
}
```

**验收标准：**
- [x] 提示词链可正常编排
- [x] 链式调用成功率>95%
- [ ] 可视化编排界面可用（待前端实现）
- [x] 预设链模板可用

### 16.5 测试与部署

#### 单元测试

```bash
# 运行后端测试
cd backend
pytest tests/test_few_shot.py
pytest tests/test_ab_test.py
pytest tests/test_cot_engine.py
pytest tests/test_prompt_evaluator.py
pytest tests/test_prompt_chain.py
```

#### 集成测试

```bash
# 测试Few-Shot示例
curl -X POST http://localhost:8000/api/prompts/few-shot \
  -H "Content-Type: application/json" \
  -d '{"prompt_id":"xxx","scenario":"conversation_summary","input_example":"...","output_example":"..."}'

# 测试A/B实验
curl -X POST http://localhost:8000/api/ab-experiments \
  -H "Content-Type: application/json" \
  -d '{"name":"测试实验","prompt_id":"xxx","version_a_id":"xxx","version_b_id":"xxx"}'

# 测试CoT模板
curl -X POST http://localhost:8000/api/search/ai \
  -H "Content-Type: application/json" \
  -d '{"question":"Vue3和React的区别","cot_type":"compare_analyze"}'

# 测试提示词链
curl -X POST http://localhost:8000/api/prompt-chains/xxx/execute \
  -H "Content-Type: application/json" \
  -d '{"raw_content":"文章内容..."}'
```

### 16.6 成本估算

**开发成本：**
- Phase 1：2-3天
- Phase 2：3-4天
- Phase 3：3-5天
- **总计：8-12天**

**AI调用成本：**
- Few-Shot示例会增加Token消耗（约+20%）
- CoT推理会增加Token消耗（约+30%）
- A/B测试会双倍调用（测试期）
- **预估月成本增加：50-80%**

### 16.7 版本说明

每个Phase完成后需要：
1. 运行完整测试套件
2. 提交代码并推送
3. 编写中文版本说明
4. 更新文档

**版本命名规范：**
- Phase 1: v1.9.0 - Few-Shot示例库与A/B测试基础
- Phase 2: v2.0.0 - CoT思维链与效果评估系统
- Phase 3: v2.1.0 - 提示词链编排引擎

